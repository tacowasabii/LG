# 미완성 기능 체크리스트

2026-08-20 코드 조사. 위에서부터 하나씩 처리한다.

## 전제 (두 번 바뀌었다)

**STT·TTS API 키는 없다.** 그쪽은 브라우저 API로 메운다.

**Bedrock이 붙었다** (`44c058e`) — [config.py](../backend/config.py), 기본 모델
`us.anthropic.claude-haiku-4-5`. 이 모델은 이미지 입력을 받으므로 "비전 모델이
없다"는 처음 전제가 깨졌다. 자격증명이 없으면 EXAONE으로 돌아간다.

**기획이 바뀌었다** (`c15506e`) — 전원 확인 구조를 버렸다. `verification.py`,
`gap_detector.py`, `routers/gaps.py`, `VerifyPage`, `GapsPage`가 삭제되고
"추억을 만들면 즉시 게시 + 원할 때 기억 더하기"로 대체됐다. 확인 상태 4단계는
기억 상태 3단계(`alone`/`shared`/`varied`)가 됐다. 아래에서 A5가 이 변경으로
폐기됐다.

## 지금 회귀 상태 (2026-08-20 재확인)

11개 스위트 전부 통과: `memories(6)` `media_person_tags(6)` `transcript_source(7)`
`video_upload(4)` `events_and_voice(11)` `family_visibility(21)` `permissions(11)`
`film(9)` `chat_search(14)` `chat_graph(8)` `interview_extraction(8)`.
Postgres 경로(`test_store_pg`)와 `npm run build`는 안 돌렸다.

---

## A. 지금 한다 — 외부 의존 없음

### [x] A1. 사진에 인물 태그하기 — 완료 (`1aadb74`)

- `PUT /api/media/{id}/persons` — 보낸 목록이 최종 상태 (더하기·빼기 모두)
- `POST /api/media/upload` 의 `person_ids` 폼 필드
- `event_resolver.set_media_persons()` — `detected_faces`와 DEPICTS를 함께 맞춘다
- `_unlink_media_from_person()` — `remove_edge`가 두 노드 사이 관계를 **모두**
  지우므로, 뗄 때 NARRATED_BY가 함께 사라지는 것을 막는다
- CollectPage에 "이 사진에 누가 있나요?" 알약
- `tests/test_media_person_tags.py` 6개

더하기만 두지 않은 이유: 등장 인물이 비공개 판정에 쓰이는데 잘못 지목한 사람을
뗄 수 없으면 남의 사진에 자기가 영구히 붙는다.

추억 리팩터링 이후에도 살아 있다. CollectPage가 새로 쓰였지만 태그 UI와
`setMediaPersons` 호출이 그대로 옮겨졌고, AI 인물 추정을 "네"로 받을 때도 같은
경로를 쓴다 (`acceptCandidate`).

### [x] A2. 음성을 글로 — 브라우저 STT — 완료 (`658e6b0`)

- [transcriber.ts](../frontend/src/lib/transcriber.ts) — Web Speech API. 녹음과
  나란히 돌고, 조용해서 끊기면 다시 듣는다. 실패해도 녹음은 남는다
- `transcript_source` 축 신설 (`ai_stt` | `user_input`). 목소리의 출처(`source`)와
  따로 둔다 — 사람이 한 글자라도 고치면 `user_input`
- AudioClip이 "AI가 옮긴 글입니다. 목소리가 원본입니다"로 밝힌다
- `tests/test_transcript_source.py` 7개

**곁에서 찾은 버그**: 브라우저 녹음이 음성이 아니라 영상으로 저장되고 있었다.
`.webm`이 영상 확장자와 같아서 `media_type=audio` 필터에서 통째로 빠졌다.
브라우저가 보내는 `audio/webm`을 믿도록 고쳤다.

전사기가 새 `MemoryComposer`에도 채택됐다 — 추억에 기억을 더할 때도 말로 남긴다.

### [x] A3. 영상 썸네일 · 길이 — 완료

- [videoMeta.ts](../frontend/src/lib/videoMeta.ts) — 브라우저가 `<video>`+canvas로
  길이와 첫 장면(JPEG, 300px)을 뽑는다. 첫 프레임이 검은 영상이 많아 10% 지점에서
  뽑고, 못 열거나 시간이 초과되면 얻은 것만 돌려준다
- `uploadMedia`가 `video/*`일 때만 자동으로 재서 `duration_sec`·`poster`를 함께 보낸다.
  호출부는 바뀌지 않았다 (한 곳에서만 처리한다)
- 서버는 poster를 그대로 두지 않고 Pillow로 다시 저장한다 — 정말 이미지인지,
  크기가 터무니없지 않은지를 여기서 가른다. 실패하면 썸네일만 없이 업로드는 성공한다
- 목록의 `<video>` 네 곳에 `poster` + `preload="none"` — **재생 전까지 원본을
  내려받지 않는다.** 이게 실제 이득이다
- `tests/test_video_upload.py` 4개

**고치면서 만든 회귀 하나**: `MemoryComposer`가 `thumbnail_path || file_path`를
`thumb` 하나로 합쳐 들고 있었다. 영상에 썸네일이 생기니 `<video src={JPEG}>`가
되어 미리보기가 빈다. 썸네일과 원본을 따로 들도록 나눴다.

같은 `thumbnail_path || file_path` 패턴이 화면 6곳과 백엔드 5곳에 더 있지만 그쪽은
전부 **이미지** 자리라서, 예전에 영상 파일을 `<img>`에 물려 아무것도 안 보이던 것이
이제 첫 장면으로 채워진다. `film_composer.py:105`는 이미 영상 썸네일을 기대하고
있었다 — 이제 실제로 받는다.

### [ ] A4. 지도 좌표 범위 밖 처리

[KoreaMap.tsx:33-41](../frontend/src/components/KoreaMap.tsx#L33-L41)의 투영이
한국 bbox(32.9~38.7N, 125.6~130.0E)로 하드코딩돼 있는데
[MapPage.tsx:67](../frontend/src/pages/MapPage.tsx#L67)은 좌표 유무만 본다.
**해외 여행 기록을 올리면 점이 화면 밖으로 나간다.**

### [ ] A5. ~~Gap 규칙 추가~~ — 폐기 (기획 변경)

`gap_detector.py`와 `routers/gaps.py`가 삭제됐다. "빠진 것을 목록으로 세워 가족에게
할 일을 주는" 방향 자체를 버렸으므로, 설계 문서의 네 번째 Gap 규칙을 채우는 일은
더 이상 요구가 아니다.

### [x] A6. 죽은 코드 · 잘못 안내하는 문서 — 완료

- `MockBadge.tsx`를 삭제하고 `Page`의 `mock` prop을 걷었다. 쓰는 화면이 없었다 —
  목데이터를 다 걷어낸 흔적만 남아 있던 것이다
- `.kiro/steering/api-guide.md`가 **삭제된** `GET /api/gaps`와
  `POST /api/media/supplement`를 가르치고 있었다. AI 어시스턴트가 읽는 지침이라
  없는 API를 부르는 코드가 나온다. 지금 구조(memories · 업로드 폼 필드 전체 ·
  인물 지목)로 다시 썼다
- `project-overview.md`의 라우터 목록에서 `gaps`를 뺐다
- `docs/SPEC_SUMMARY.md`에 남아 있던 supplement 항목 두 곳을 걷고, 업로드 폼이
  실제로 받는 필드를 적었다
- ~~`link_person_to_event()` 호출부 없음~~ — 추억 리팩터링에서 삭제됐다
- ~~README 검증 목록 누락~~ — 테스트 파일과 정확히 일치한다

### [x] A7. 깨진 테스트 복구 — 완료

`tests/test_interview_extraction.py`가 **import 단계에서 죽어 있었다.** 추억
리팩터링이 지운 `VerificationState`와 `verification` 모듈을 참조했다. 그 커밋이
세 스위트만 돌려서 드러나지 않았고, README 검증 목록에는 그대로 남아 있었다.
확인 상태 축을 `MemoryState`로 옮겨 8개 전부 통과시켰다.

### [ ] A8. 사진 장면 설명 (Bedrock vision) — 새로 생긴 항목

Bedrock이 붙어서 가능해졌고, 추억 초안이 이걸 필요로 한다.

[memory_drafter.py:363](../backend/services/memory_drafter.py#L363)은 초안의
"주요 내용" 입력으로 `scene_description`을 읽는다. 그런데 그 필드를 **쓰는 코드가
어디에도 없다.** 채우던 유일한 경로였던 `POST /api/media/supplement`가 추억
리팩터링에서 삭제됐고, 남은 것은 시드 스크립트뿐이다.

그래서 업로드한 사진의 `scene_description`은 영구히 비어 있다 — 초안이 사진에
무엇이 찍혔는지 전혀 모르는 채로 제목과 설명을 쓴다. 지금 쓸 수 있는 근거는 EXIF
날짜·좌표와 사람이 지목한 인물뿐이다.

읽는 곳이 넷이다: 초안 · 채팅 컨텍스트(`chat_engine.py:146` "장면: …") ·
검색 가중치(`graph_search.py:24`) · Postgres 검색 색인(`stores/base.py:50`, 주석에
*"scene_description은 사진이 가진 유일한 텍스트다"* 라고 적혀 있다).

`llm_client._to_converse` ([llm_client.py:190](../backend/services/llm_client.py#L190))는
텍스트만 다룬다. 이미지 블록을 넣는 길을 열어야 한다.

**얼굴 인식은 여기에 포함하지 않는다.** 아래 C 표 참고.

---

## B. 더 큰 것 — 범위를 정하고 들어간다

### [ ] B1. Memory Film을 실제 영상으로

[FilmPage.tsx](../frontend/src/pages/FilmPage.tsx)가 직접 적어 뒀다:
*"남은 교체 지점: 미리보기 진행바 → 실제로 렌더된 영상 플레이어"*.
`ai_effects`는 라벨이고 화면은 CSS 모션으로 흉내 낸다. 내려받을 파일이 없다.
사용자 안내서도 "장면 구성 미리보기이고, 영상 파일로 렌더하지는 않습니다"로
밝혀 둔 상태다.

canvas + MediaRecorder로 브라우저에서 webm을 만들 수 있다 (서버 인코더 불필요).
A3에서 만드는 영상 프레임 캡처와 같은 도구를 쓴다.

### [ ] B2. 내레이션 읽어주기 (TTS)

Film·TV 내레이션이 텍스트로만 나온다. 브라우저 `speechSynthesis`면 키 없이 된다.
단, **AI 합성 목소리를 가족 목소리처럼 들리게 하면 안 된다** —
[AudioClip.tsx:10](../frontend/src/components/AudioClip.tsx#L10)이 정한 선이다.

### [ ] B3. 로그인 · 인증

[permissions.py:6](../backend/services/permissions.py#L6)이 스스로 밝힌다:
*"이것은 보안 경계가 아니다. 인증이 없으므로 헤더를 직접 바꾸면 우회된다."*
[currentUser.tsx](../frontend/src/lib/currentUser.tsx)는 localStorage에 사람 id를
저장한다. 역할 가드와 공개 범위는 실수 방지 수준이고 진짜 경계는 없다.

---

## C. 여기서는 안 한다 — 이유를 적어 둔다

| 항목 | 왜 |
|---|---|
| AI Vision **얼굴 인식** | Bedrock이 붙었지만 그래도 안 한다. 범용 VLM으로 "이 사람이 김민수다"를 판정하는 것은 정확도가 아니라 성격의 문제다 — 가족 구성원 식별은 생체정보이고, 틀리면 남의 사진에 엉뚱한 사람이 붙는다. A1(사람이 직접 지목)이 이 제품에 맞는 답이다 |
| 지도 SDK (Leaflet/Mapbox) | 타일 네트워크·키·비용. A4로 버틴다 |
| webOS 실기기 | 기기·인증서 없음. [remote.ts](../frontend/src/lib/remote.ts)에 교체 지점만 남겨 둔다 |
| Export 작업 큐 | 시드 26MB에서 즉시 끝난다 ([export.py:3-5](../backend/routers/export.py#L3-L5)) |
| 벡터 검색 | 키워드 가중치로 goldset을 통과하고 있다 |
| Trust 화면에서 채점 실행 | 의도한 설계다. `POST /api/trust/run`은 있지만 몇 분 걸려서 화면에 안 붙였다 ([trust.py:3-5](../backend/routers/trust.py#L3-L5)) |
