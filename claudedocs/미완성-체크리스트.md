# 미완성 기능 체크리스트

2026-08-20 코드 조사 결과. 위에서부터 하나씩 처리한다.

가능한 것과 불가능한 것을 먼저 나눴다. STT·TTS API 키는 없으므로 그쪽은 브라우저
API로 메운다.

**2026-08-20 갱신**: 병행 작업으로 Bedrock이 붙었다 ([config.py](../backend/config.py) ·
기본 모델 `us.anthropic.claude-haiku-4-5`). 이 모델은 이미지 입력을 받으므로 "비전
모델이 없다"는 전제가 깨졌다 — 사진 장면 설명은 이제 가능하다 (아래 C 표 참고).
다만 자격증명이 없으면 EXAONE으로 돌아간다.

---

## A. 지금 한다 — 외부 의존 없음

### [x] A1. 사진에 인물 태그하기 (DEPICTS 공백 메우기) — 2026-08-20 완료

한 것:
- `PUT /api/media/{id}/persons` — 보낸 목록이 최종 상태가 된다 (더하기·빼기 모두)
- `POST /api/media/upload` 에 `person_ids` 폼 필드 (올릴 때 한 번에)
- `event_resolver.set_media_persons()` — detected_faces와 DEPICTS를 함께 맞춘다
- `_unlink_media_from_person()` — `remove_edge`가 두 노드 사이 관계를 **모두**
  지우므로, 뗄 때 NARRATED_BY가 함께 사라지는 것을 막는다
- CollectPage에 "이 사진에 누가 있나요?" 알약 (음성 제외)
- `tests/test_media_person_tags.py` 6개 — 6/6 통과
- 곁에서 고친 것: CollectPage의 `MEDIA_TYPE_LABEL`이 `image` 키를 보고 있어
  사진 알약에 `photo`가 그대로 노출되고 있었다

더하기만 두지 않은 이유: 등장 인물이 비공개 판정에 쓰이는데 잘못 지목한 사람을
뗄 수 없으면, 남의 사진에 자기가 영구히 붙는다.

<details>
<summary>원래 조사 내용</summary>

가장 큰 기능 구멍이다. 업로드한 사진은 **어떤 인물과도 이어지지 않는다.**

- `detected_faces`는 [seed_from_metadata.py:180](../scripts/seed_from_metadata.py#L180)에서만 채워진다
- 업로드 폼에 `person_ids`가 없다 ([media.py:23-37](../backend/routers/media.py#L23-L37))
- 보충 API도 날짜·이벤트·설명만 받는다 ([schemas.py:42-48](../backend/models/schemas.py#L42-L48))
- `link_media_to_person()` ([event_resolver.py:217](../backend/services/event_resolver.py#L217))은 호출부가 없는 죽은 코드

파급: 인물별 미디어 조회([media.py:181](../backend/routers/media.py#L181)), 등장인물 기준
비공개 판정([visibility.py:97](../backend/services/visibility.py#L97)), Chat 근거가 모두 시드
데이터에만 얹혀 있다. 가족이 새로 올린 사진은 그래프에서 고아가 된다.

할 일:
- 업로드에 `person_ids` (Form, 쉼표 구분) → 인물 노드 검증 → `detected_faces` + DEPICTS 엣지
- 보충 요청에 `person_ids` → 같은 처리
- confidence는 `user_input` / `confirmed` (사람이 직접 지목한 것이므로)
- CollectPage에 인물 고르는 UI

</details>

### [x] A2. 음성을 글로 — 브라우저 STT — 2026-08-20 완료

한 것:
- `frontend/src/lib/transcriber.ts` — Web Speech API 래퍼. 녹음과 나란히 돌고,
  조용해서 스스로 끊기면 다시 듣는다. 실패해도 녹음은 끝까지 남는다
- InterviewPage — 말하는 동안 입력란이 채워지고, 확정 전 조각은 밖에 흐리게
- `transcript_source` 축 신설 (`ai_stt` | `user_input`). 목소리의 출처(`source`)와
  따로 둔다 — 사람이 한 글자라도 고치면 user_input이 된다
- AudioClip이 "AI가 옮긴 글입니다. 목소리가 원본입니다."로 밝힌다
- 화면이 주장하는 출처를 그대로 믿지 않는다. `ai_stt` 아닌 값은 user_input으로

**곁에서 찾은 버그 (이게 더 컸다)**: 브라우저 녹음이 **음성이 아니라 영상으로
저장되고 있었다.** `detect_media_type`이 확장자만 보는데 `.webm`이 영상 목록에
있고, `uploadVoice`는 `voice-*.webm`으로 올린다. 그래서 녹음한 목소리가
`media_type=audio` 필터에서 통째로 빠졌다 — 음성 목록·인물 상세·AudioClip에
아무것도 나오지 않는다. 기존 테스트는 `MediaNode(media_type=AUDIO)`를 직접
만들어서 이 경로를 지나지 않았다. 브라우저가 보내는 `audio/webm`을 믿도록 고쳤다.
지금 그래프에 잘못 들어간 녹음은 없다 (아직 업로드된 적이 없다).

`tests/test_transcript_source.py` 7개 — 7/7 통과.

<details>
<summary>원래 조사 내용</summary>

지금은 **타이핑 없이 음성만으로 답변할 수 없다.** 제출 버튼이 텍스트를 요구한다
([InterviewPage.tsx:507](../frontend/src/pages/InterviewPage.tsx#L507)). `transcript`로 저장되는 것은
사용자가 손으로 쓴 답변이다 ([InterviewPage.tsx:168](../frontend/src/pages/InterviewPage.tsx#L168)).

STT API 키가 없으므로 브라우저 Web Speech API(`SpeechRecognition`)로 녹음 중에
전사한다. 키·비용 없고 크롬에서 동작한다. 인식이 안 되는 브라우저에서는 지금처럼
타이핑으로 떨어진다.

할 일:
- [recorder.ts](../frontend/src/lib/recorder.ts)에 인식 결과 누적
- 전사문을 답변 입력란에 채우고 사람이 고칠 수 있게
- 브라우저가 지원하지 않으면 조용히 기존 경로

</details>

### [ ] A3. 영상 썸네일 · 길이

업로드한 video는 썸네일도([media.py:64](../backend/routers/media.py#L64) — photo만),
길이도 없다. 목록·그래프에서 영상은 회색 칸으로 뜨거나 원본을 통째로 로드한다.

음성 파형과 같은 패턴을 쓴다 — 브라우저가 첫 프레임을 캡처하고 duration을 재서
보낸다 (서버에 디코더를 두지 않는다는 기존 결정을 따른다).

### [ ] A4. 지도 좌표 범위 밖 처리

[KoreaMap.tsx:33-41](../frontend/src/components/KoreaMap.tsx#L33-L41)의 투영이 한국
bbox(32.9~38.7N, 125.6~130.0E)로 하드코딩돼 있는데
[MapPage.tsx:66-72](../frontend/src/pages/MapPage.tsx#L66-L72)는 범위를 걸러내지 않는다.
**해외 여행 기록을 올리면 점이 화면 밖으로 나간다.**

### [ ] A5. Gap 규칙 하나 추가 — 사건에 미연결인 미디어

[MVP_DESIGN.md](../docs/MVP_DESIGN.md) 2.5의 네 규칙 중 "시간적으로 가까운 Media가
Event에 미연결"만 없다. [gap_detector.py](../backend/services/gap_detector.py)는 6종을
탐지하는데 이 유형이 빠져 있다.

### [ ] A6. 죽은 코드 · 문서 정리

- `MockBadge`를 쓰는 페이지가 없다 ([Page.tsx:35](../frontend/src/components/Page.tsx#L35)) — 목데이터 걷어내기가 끝난 흔적
- `link_person_to_event()` / `link_media_to_person()` 호출부 없음 (A1에서 살릴 수 있다)
- README "검증" 절에 `tests/test_interview_extraction.py` 누락

---

## B. 더 큰 것 — 범위를 정하고 들어간다

### [ ] B1. Memory Film을 실제 영상으로

[FilmPage.tsx:16](../frontend/src/pages/FilmPage.tsx#L16)이 직접 적어 뒀다:
*"남은 교체 지점: 미리보기 진행바 → 실제로 렌더된 영상 플레이어"*.
지금 `ai_effects`는 라벨이고([film_composer.py:97](../backend/services/film_composer.py#L97))
화면은 CSS 모션으로 흉내 낸다. 내려받을 파일이 없다.

canvas + MediaRecorder로 브라우저에서 webm을 만들 수 있다 (서버 인코더 불필요).

### [ ] B2. 내레이션 읽어주기 (TTS)

Film·TV 내레이션이 텍스트로만 나온다. 브라우저 `speechSynthesis`면 키 없이 된다.
단, **AI 합성 목소리를 가족 목소리처럼 들리게 하면 안 된다** —
[AudioClip.tsx:10](../frontend/src/components/AudioClip.tsx#L10)이 정한 선을 지켜야 한다.

### [ ] B3. 로그인 · 인증

[permissions.py:6](../backend/services/permissions.py#L6)이 스스로 밝힌다:
*"이것은 보안 경계가 아니다. 인증이 없으므로 헤더를 직접 바꾸면 우회된다."*
[currentUser.tsx:12](../frontend/src/lib/currentUser.tsx#L12)는 localStorage에 사람 id를
저장한다. 역할 가드와 공개 범위는 실수 방지 수준이고 진짜 경계는 없다.

---

## C. 여기서는 안 한다 — 이유를 적어 둔다

| 항목 | 왜 |
|---|---|
| AI Vision **얼굴 인식** | Bedrock이 붙었지만 그래도 안 한다. 범용 VLM으로 "이 사람이 김민수다"를 판정하는 것은 정확도가 아니라 성격의 문제다 — 가족 구성원 식별은 생체정보이고, 틀리면 남의 사진에 엉뚱한 사람이 붙는다. A1(사람이 직접 지목)이 이 제품에 맞는 답이다 |
| AI Vision **장면 설명** | 이제 가능해졌다. `scene_description`은 지금 EXIF 없는 사진에서 사람이 적는 것뿐이다 ([media.py supplement](../backend/routers/media.py)). 새 항목으로 승격할 만하다 — A7 후보 |
| 지도 SDK (Leaflet/Mapbox) | 타일 네트워크·키·비용. A4로 버틴다 |
| webOS 실기기 | 기기·인증서 없음. [remote.ts](../frontend/src/lib/remote.ts)에 교체 지점만 남겨 둔다 |
| Export 작업 큐 | 시드 26MB에서 즉시 끝난다 ([export.py:3-5](../backend/routers/export.py#L3-L5)) |
| 벡터 검색 | 키워드 가중치로 goldset을 통과하고 있다 |
| Trust 화면에서 채점 실행 | 의도한 설계다. `POST /api/trust/run`은 있지만 몇 분 걸려서 화면에 안 붙였다 ([trust.py:3-5](../backend/routers/trust.py#L3-L5)) |
