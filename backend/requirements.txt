fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
networkx==3.3
pillow==10.4.0
exifread==3.0.0
httpx==0.27.0
pydantic>=2.9.0
python-dotenv==1.0.1
aiofiles==24.1.0

# LLM 오케스트레이션 (Memory Chat 질의 계획)
# langchain 1.x / openai 3.x 는 최근 메이저 버전이라 정확히 고정한다.
langgraph==1.2.11
langchain-core==1.5.6
langchain-openai==1.5.2

# 저장소 (DATABASE_URL이 있을 때만 실제로 쓰인다)
psycopg[binary]==3.2.3
psycopg-pool==3.2.3

# Bedrock (자격증명이 있을 때만 실제로 쓰인다)
boto3==1.43.75

# 미세 모션 클립 생성 (MOTION_AUTOGEN=true 일 때만 실제로 호출한다).
# 끄고 쓰면 import되지 않지만, 배포에서 켜려면 이미지에 있어야 한다.
fal-client==1.0.1
